spring:
  datasource:
    url: jdbc:mysql://localhost:3307/gestao_database?allowPublicKeyRetrieval=true&useSSL=false&serverTimezone=UTC
    username: gestao-dev
    password: gestao-tarefa
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

  flyway:
    enabled: true
    baseline-on-migrate: true
